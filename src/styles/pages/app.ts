import { styled } from "..";

export const Container = styled("div", {
  // display: "",
  // flexDirection: "column",
  // alignItems: "flex-start",
  // justifyContent: "center",
  minHeight: "100vh",
});
